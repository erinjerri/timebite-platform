# Architecture

Production-oriented system architecture for TimeBite.

Goals:
- Keep the iOS app shippable and local-first.
- Define clear boundaries so backend sync can be added later without rewrites.
- Prefer simple, testable interfaces over premature microservices.

