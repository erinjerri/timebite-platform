# Apps

Client applications live here.

Current state:
- `apps/ios/` is the intended iPhone-first app root (no Xcode project committed yet).
- `apps/iOS/`, `apps/visionOS/`, `apps/macoS/` exist as early placeholders and should be normalized over time.

Direction:
- iPhone app launches first.
- watchOS/macOS/visionOS follow after the core loop is proven and stable.
